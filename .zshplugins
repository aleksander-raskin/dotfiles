### VS code
source <(kubectl completion zsh)
source /usr/local/share/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh

### Zplub
export ZPLUG_HOME=/usr/local/opt/zplug
source $ZPLUG_HOME/init.zsh

zplug 'wfxr/forgit'

# Then, source plugins and add commands to $PATH
#zplug load

####
[ -f ~/.fzf.zsh ] && source ~/.fzf.zsh

complete -o nospace -C /usr/local/bin/vault vault

# The next line updates PATH for the Google Cloud SDK.
if [ -f '/Users/raskin/google-cloud-sdk/path.zsh.inc' ]; then . '/Users/raskin/google-cloud-sdk/path.zsh.inc'; fi

# The next line enables shell command completion for gcloud.
if [ -f '/Users/raskin/google-cloud-sdk/completion.zsh.inc' ]; then . '/Users/raskin/google-cloud-sdk/completion.zsh.inc'; fi
